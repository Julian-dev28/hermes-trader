# manta_strategy — reverse-engineer the MANTA +100% win into a setup, test the universe

Trace: MANTA had NO precursor — vol was EXPANDING into the move (compression 2.06, not a coil),
base 23.8% (not tight); the only signal is the coincident IGNITION bar (65x volume surge, +50% candle).

Strategy test (volume-surge breakout = new 48h high + vol >= Vx), universe, fwd 48h net 12bps:
  5x  n=236 -0.04% (8% ran>=20%, 0% >=50%) | 10x n=86 -0.93% | 30x n=11 -6.36%/18%win | 65x n=2 (MANTA rare)
VERDICT: does NOT generalize. -EV to breakeven; BIGGER surge = WORSE (high spikes = exhaustion/distribution,
they reverse). MANTA-level (65x) ~1-in-thousands. No threshold makes it robustly +EV.

Conclusion: MANTA is a RIGHT-TAIL lottery outcome of the breakout-long, not a detectable repeatable pattern.
You catch it via the TA-confirmed breakout entry (small size, AS it ignites) + scale-out/trail riding the rare
runner; the same entry eats many fizzles. The EDGE IS THE EXIT, not a predictive entry. Reconfirms
[[project_early_runner_no_tell]] + [[project_price_entries_no_edge]] + the KAITO exit study.
